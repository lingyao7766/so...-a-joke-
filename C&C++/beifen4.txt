#include <bits/stdc++.h>

int tao(int day)
{
    int n1=1;
    if(n1==day)
        return 1;
    else
        return 2*(tao(n1+1)+1);
}

int main()
{
    int x=1,n;
    int i=1;
    std::cin>>n;
    while(i<n){
        x=(x+1)*2;
          i++;
    }
    int x1=tao(n);
    std::cout<<x1;
    return 0;
}

