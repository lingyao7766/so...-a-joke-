python 和C/C++基本语法上最显著的区别在于——括号和引号的不同

比如 C/C++ 几乎每行独立代码后都要添加一个引号；
而 python 在句尾则几乎从来没有带过引号。

python在循环甚至if分支里从未加过括号，且经常性的以冒号为指令；
C/C++则在循环或分支中经常性的把判断条件用括号括起来。



python和C/C++另一个重要区别在于——list、temple和数组

即，python是没有所谓“数组”的。